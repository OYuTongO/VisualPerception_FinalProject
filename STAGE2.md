# 游戏模块第二阶段任务

> 说明：以下文档已将用户的第二阶段目标整理为适合大模型阅读、拆解、执行和验收的任务说明。

---

## 阶段目标

在第一阶段可运行 Demo 的基础上，将当前“聊天式 ASL 识别演示”推进为更成熟、更有游戏感、更具技术表现力的 ASL 学习游戏模块。

当前已知前提：
- 现有 ASL 识别链路已经跑通。
- `src/recognizer.py` 的识别与模型逻辑已经验证可用。
- 第一阶段 Pygame 版本聊天式 Demo 已可运行。
- 角色头像素材目录已从 `pnz/` 更名为 `game_module/`。
- 第二阶段重点不是重写识别模型，而是在游戏层、角色表现层、UI 动效层、反馈机制层大幅提升完成度。

---

## 第二阶段核心目标

### 1. 显著提升游戏画面精美度

当前游戏不应再停留在基础 Demo 或静态界面状态。第二阶段需要让画面本身更接近一个完整游戏：

- 背景要有明显的流动感、科技感和层次感。
- 视觉风格应参考原游戏背景特征，并在此基础上增强动态表现。
- Diana 和“我”两个角色不能只是单一静态头像，需要具备更强的动态性。
- 整体界面要看起来像一个经过设计的互动游戏，而不是普通 UI 拼接。

可考虑的表现方向：
- 动态科技网格。
- 流动扫描线。
- 发光粒子。
- 背景视差层。
- 角色呼吸动画、轻微浮动、状态切换动画。
- 气泡、字母槽、反馈符号具有动效和发光效果。

---

### 2. 强化“技术性可视化”表达

第二阶段需要让玩家直观看到识别技术正在参与游戏，而不是只看到文字变色。

核心要求：

#### 2.1 字母识别后的入槽动画

当玩家在摄像头前正确比划出一个目标字母时，不只是简单把灰色字母变成金色，而是播放一个更有表现力的动画：

```text
识别到正确字母
  ↓
该字母以大号形态最大化进入屏幕中央或显眼位置
  ↓
带有科技感发光、缩放、轨迹或粒子效果
  ↓
逐步缩小并飞回对应对话框中的字母槽
  ↓
填满原本灰色的字母槽，变为金色完成态
```

目标体验：玩家能明确感知“我比划出的 ASL 字母被系统识别、确认、转化并填入对话”。

---

#### 2.2 “我”的角色动作同步

当玩家在摄像头前做 ASL 手势时，界面中“我”对应的角色也应尽量做出相同或相近的动作。

理想目标：
- 玩家比划字母 A，角色 X 也展示 A 的手语动作。
- 玩家比划字母 B，角色 X 也展示 B 的手语动作。
- 角色动作应与当前识别结果联动。

允许的阶段性实现方式：
- 若没有完整骨骼动画资源，可先使用每个字母对应的静态手势图或半身角色图。
- 若素材不足，可先实现“角色头像 + 当前识别字母手势图”的组合表现。
- 后续再升级为完整角色动画或多帧序列。

---

#### 2.3 Diana 的手语示范动作

Diana 不应只是发出文字提示。她也应该以手语动作表达她想说的内容。

目标：
- Diana 说出的每一个英文字母，都可以通过正确的 ASL 手势动作依次展示。
- Diana 的文字信息进入对话气泡时，也可以配套出现字母槽或手势展示。
- Diana 作为教学者，应具备“示范能力”。

建议设计：

```text
Diana 发出一句话
  ↓
文字自动输入到气泡中
  ↓
Diana 依次展示该句中关键字母或目标单词的 ASL 手势
  ↓
对应字母进入 Diana 的字母槽或展示区
```

注意：
- Diana 的示范不需要依赖摄像头识别。
- Diana 的动作可由预设资源、字母手势图、动画序列或程序化角色姿态驱动。

---

#### 2.4 玩家错误时 Diana 必须示范正确动作

当检测到玩家某个字母比划错误时，不能只显示红色 `×`。

新的错误反馈流程应为：

```text
当前目标字母为 T
  ↓
玩家比划错误，系统识别为其他字母
  ↓
界面显示红色 ×，错误次数 +1
  ↓
暂停玩家继续输入
  ↓
Diana 展示目标字母 T 的正确 ASL 手势
  ↓
出现提示文字，例如："Watch Diana sign T. Try again."
  ↓
示范完成后，玩家才能继续比划当前目标字母
```

这个机制的目的：
- 错误反馈不只是惩罚，而是教学。
- Diana 作为引导者，能在玩家犯错时提供正确示范。
- 游戏从“识别测试”升级为“可学习的交互体验”。

---

## 用户原始需求摘要

用户对第二阶段提出的核心要点如下：

```text
1. 游戏画面本身要足够精美，包括戴安娜和“我”人物的动态性，以及背景的流动感与科技感。既然要这样做，就不能再用单一的静态任务图片，并且要参考原游戏的背景特征。

2. 技术性一定要充分体现。比如比划出的每个字母可以做一个效果：先最大化进入屏幕，然后逐步缩小回到对话框，填满灰色的字母槽。

3. 玩家在摄像头前面做动作时，尽量做到让“我”对应的那个角色同时做出相同的动作。

4. 与之相应，戴安娜也应该用正确的手语动作把她想表达的每一个字母比划出来，再呈现在字母槽中。

5. 如果检测到某一个字母玩家比划错误，戴安娜要比划出正确的字母手语作为示范，之后玩家才能继续比划。
```

---

## 大模型执行指令

请在阅读本阶段需求时遵循以下原则：

1. **保持识别主链路稳定**
   - 不要随意重写已验证通过的识别模块。
   - 不要随意修改 `src/recognizer.py`、模型文件、训练逻辑或关键点提取逻辑。
   - 若需要增强交互，应优先在游戏层、UI 层、动画层、资源层扩展。

2. **以游戏体验和技术展示为中心**
   - 第二阶段的重点是让用户“看到技术在工作”。
   - 每次识别成功、识别失败、角色示范都应有清晰的视觉反馈。
   - UI 不应只是文字变化，而应通过动画表达识别、转化、教学和确认过程。

3. **角色动态性优先于静态头像**
   - `game_module/D.jpg` 和 `game_module/X.jpg` 可继续作为基础头像或角色主视觉。
   - 但第二阶段应逐步引入更多角色状态或动作资源。
   - 若没有完整动画素材，应设计可扩展的资源命名规则和降级方案。

4. **Diana 是教学者，不只是聊天对象**
   - Diana 应能主动演示正确 ASL 字母。
   - Diana 在玩家错误后必须展示正确动作。
   - Diana 的示范流程应成为游戏教学闭环的一部分。

5. **输出应结构化、可执行、可验收**
   - 每项任务应明确目标、改动点、依赖资源、验收标准。
   - 能拆分为子任务时，优先拆分。

6. **避免过度设计**
   - 不为了未来假设需求引入过重架构。
   - 第二阶段应优先完成关键体验闭环，再逐步升级美术与动画。

7. **资源路径统一使用新目录**
   - 角色头像、手势示范图、角色状态图、背景素材等游戏相关资源统一从 `game_module/` 读取。

---

## 建议模块拆分

### 1. 资源层：`game_module/`

建议将游戏相关素材统一放入：

```text
game_module/
  D.jpg
  X.jpg
  gestures/
    A.png
    B.png
    C.png
    ...
  diana/
    idle.png
    sign_A.png
    sign_B.png
    ...
  player/
    idle.png
    sign_A.png
    sign_B.png
    ...
  background/
    ...
```

阶段性可先只实现 `gestures/A-Z.png` 或部分字母资源。

---

### 2. 动效层

建议新增或扩展动画管理能力：

- 字母飞入字母槽动画。
- 红色错误反馈动画。
- 绿色完成反馈动画。
- 角色呼吸 / 浮动动画。
- Diana 示范动作播放。
- 玩家角色根据识别结果切换动作。

---

### 3. 角色表现层

需要让 Diana 和 X 从“头像”升级为“可表现状态的角色”。

建议角色状态：

```text
IDLE        等待
TALKING     正在输出文字
SIGNING     正在比划某个字母
CORRECT     玩家正确时鼓励
WRONG       玩家错误时纠错
TEACHING    Diana 正在示范正确手势
```

---

### 4. 字母槽与识别反馈层

当前灰字变金色的逻辑需要升级为“字母槽”。

每个目标字母应有独立状态：

```text
PENDING     灰色待填充
ACTIVE      当前目标，高亮
FLYING      正确识别后正在飞入
FILLED      金色完成
ERROR       当前错误反馈
```

---

### 5. 错误教学流程层

需要新增错误后的 Diana 示范状态。

推荐状态机：

```text
WAIT_SIGN
  ↓ 正确
LETTER_FLY_IN
  ↓
WAIT_SIGN / ROUND_DONE

WAIT_SIGN
  ↓ 错误
WRONG_FEEDBACK
  ↓
DIANA_DEMO_CORRECT_LETTER
  ↓
WAIT_SIGN
```

在 `DIANA_DEMO_CORRECT_LETTER` 期间，玩家输入应暂停，避免识别继续推进。

---

## 第二阶段验收标准

第二阶段完成后，应至少满足：

- 背景具有明显动态科技感，而不是静态纯色或简单网格。
- Diana 和 X 至少具备基础动态表现，例如呼吸、浮动、状态切换或手势图切换。
- 玩家正确比划字母后，字母不只是变色，而是经过“放大出现 → 缩小飞入 → 填入字母槽”的动画。
- 玩家角色 X 能根据识别结果显示对应动作或对应字母手势展示。
- Diana 能展示她要表达或示范的正确 ASL 字母。
- 玩家比划错误时，必须先显示错误反馈，再由 Diana 示范正确字母，示范结束后玩家才能继续。
- 所有新增资源统一从 `game_module/` 读取。
- 现有识别与模型逻辑保持不变。

---

## 风险与阶段性降级方案

### 风险 1：缺少完整角色动画素材

降级方案：
- 先使用静态字母手势图代替完整角色动作。
- Diana/X 头像继续保留，但旁边显示对应字母的手势图。
- 后续再替换为完整角色动作序列。

### 风险 2：所有字母都做角色动作成本较高

降级方案：
- 优先覆盖当前对话脚本中会出现的字母。
- 其他字母临时使用通用占位图或文字提示。

### 风险 3：动画系统复杂度上升

降级方案：
- 第二阶段先实现单个字母飞入动画。
- 后续再扩展粒子、轨迹、缓动曲线和多动画并发。

### 风险 4：摄像头识别与动画状态冲突

降级方案：
- 在 Diana 示范、字母飞入、回合完成期间暂停消费识别结果。
- 只在 `WAIT_SIGN` 状态处理玩家输入。

---

## 1. 本阶段新完成事项

- 将角色素材目录从 `pnz/` 更名为 `game_module/`。
- 已同步更新当前代码与文档中的头像资源引用路径：
  - `src/game/learn_mode.py`
  - `src/utils/draw.py`
  - `README.md`
- 已创建并更新 `STAGE2.md`，作为第二阶段任务说明文档。
- 已将第二阶段目标整理为适合大模型继续执行的结构化需求。
- 已明确第二阶段重点：画面精美度、角色动态性、科技感背景、字母飞入动画、玩家角色动作同步、Diana 手语示范、错误后的教学闭环。

---

## 2. 之后阶段待完成事项

- 收集或生成 Diana 与 X 的角色动态素材。
- 收集或生成 A-Z 静态/动态 ASL 手势示范资源，并排除当前模型不支持的 J/Z。
- 实现字母槽状态系统。
- 实现正确识别后的字母放大、缩小、飞入、填槽动画。
- 实现玩家角色 X 根据识别字母切换手势展示。
- 实现 Diana 对自己文字内容的 ASL 字母示范。
- 实现错误后的 Diana 正确字母示范流程，并在示范期间暂停玩家继续输入。
- 升级背景动效，使其具有更明显的流动感、科技感和游戏视觉层次。
- 完成第二阶段后，再规划第三阶段的音效、关卡、评分、剧情、菜单和完整游戏流程。
